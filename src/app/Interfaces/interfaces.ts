export interface Interfaces {}

export interface CreateWorkout {
  title: string;
  description: string;
  id: string;
}

export interface Workout {
  title: string;
  description: string;
  id: string;
}
